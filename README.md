# Gentle Cup Coffee - Vercel Ready

This folder includes the 4 required root items:

- package.json
- index.html
- public/
- src/

Upload these contents to GitHub/Vercel as the project root.

## Local test

cmd /c npm install
cmd /c npm run dev

Open the URL Vite prints, usually:
http://localhost:5173
